import PaintingDetail from "@/components/PaintingDetail";

export default function TVInTheLand() {
  return (
    <PaintingDetail
      title="TV in the Land"
      year="2024"
      medium="Acrylic on canvas"
      dimensions='20" × 24"'
      image="/images/tvinland.jpeg"
      statement="A television set placed in an open landscape — no cord, no outlet, no explanation. The image came from thinking about how screens have colonized every environment, including the ones we think of as untouched. The land doesn't care. The TV doesn't either. They just coexist, which is maybe the whole problem."
    />
  );
}
